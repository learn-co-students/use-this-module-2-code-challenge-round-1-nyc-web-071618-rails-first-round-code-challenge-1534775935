<h2>All Heroines</h2>

<%= form_tag(heroines_path method: :get) do %>
  Search <%= text_field_tag :name, params[:name]%>
  <%= submit_tag 'Search' %>
<% end %>, name: nil



<ul>
  <% @heroines.each do |heroine| %>
  <li>
      <%= link_to heroine.super_name, heroine_path(heroine) %>
    </li>
    <br>
  <% end %>
</ul>
